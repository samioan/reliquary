import { makeStyles } from "@material-ui/core/styles";

const styles = makeStyles(() => ({
  container: {
    padding: 8,
  },
  tooltip: {
    top: 16,
  },
}));

export { styles };
export default styles;
