import { makeStyles } from "@material-ui/core/styles";

const styles = makeStyles(() => ({
  container: {
    width: "100%",
    marginRight: 16,
  },
  bottomMargin: {
    marginBottom: 8,
  },
}));

export { styles };
export default styles;
