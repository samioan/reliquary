import { makeStyles } from "@material-ui/core/styles";

const styles = makeStyles(() => ({
  bottomMargin: {
    marginBottom: 8,
  },
  dividingMargin: {
    marginBottom: 16,
  },
}));

export { styles };
export default styles;
