import { makeStyles } from "@material-ui/core/styles";

const styles = makeStyles(() => ({
  tabs: {
    marginBottom: 16,
    padding: "0px 8px",
  },
  tooltip: {
    top: 16,
  },
}));

export { styles };
export default styles;
